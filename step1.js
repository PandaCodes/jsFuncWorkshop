function upperCaser(input) {
  return input.toUpperCase();
}


module.exports = upperCaser;

// module.exports.upperCaser = upperCaser;

// const upperCaserFunction = require('../src/step1').upperCaser

// export default const upperCaser = input => input.toUpperCase();
// import upperCaser from '../src/step1'

// export const upperCaser1 = input => input.toUpperCase();
// export const upperCaser2 = input => input.toUpperCase();
// import {upperCaser1, upperCaser2} from '../src/step1'
